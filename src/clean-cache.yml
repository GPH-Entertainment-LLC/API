resources:
  vendor-pack:
    /vendor/packs/{vendorId}*
    /vendor/packs/profile/{vendorId}*
    /vendors/packs*
    /vendors*
  vendor:
    vendor_{vendorId}
    vendor_categories
    vendor_categories_{vendorId}
  user-token:
    token_balance_{uid}
  pending-application:
    pending_application_{uid}
  rejected-application:
    rejected_application_{uid}
  approved-application:
    approved_application_{uid}
  vendor-item:
    /vendor/items/{vendorId}*
  user-item:
    user_item_amount_{uid}
    user_items_{uid}
    /user/items/{uid}*
  item:
    item_{itemId}
  item-category:
    category_item_categories
  pack-category:
    category_pack_categories
  pack-config:
    /vendors/packs* --shop packs
    /vendor/packs/profile/{vendorId}* --creator profile packs
    /vendor/packs* --vendor list packs
    pack_config_{packConfigId}
  pack-shop:
    /vendors/packs*
		/vendors*
  user-pack:
    /user/packs/{uid}*
  pack:
    pack_{packId}
  referral:
    active_vendor_referral_codes_{vendorId}
  referral-codes:
    active_referral_codes_{referralCode}
  user:
    user_username_{username}
    private_user_{uid}
    user_{uid}
    /vendor/{uid}
    /vendors*
  user-favorite:
    /user/favorites/{uid}*
    {uid}_favorite_{vendorId}

services:
  admin:
    approve-vendor:
      *
    reject-vendor:
      rejected-application
    remove-vendor:
      *
  item:
    create-item:
      vendor-item
    add-item-category:
      item-category
    delete-item:
      item
      vendor-item
  pack:
    create-pack-config:
      vendor-pack
      vendor
    generate-packs:
      vendor-pack
      vendor
    add-pack-categories:
      pack-category
    buy-packs:
      user-token
      vendor-pack
      user-pack
      pack-config
    open-pack:
      pack
    patch-pack-config:
      pack-config
    activate-packs:
      vendor-pack
      vendor
    deactivate-packs:
      vendor-pack
      vendor
    delete-pack-configs:
      vendor-pack
      vendor
      pack-config
  referral:
    generate-code:
      referral
    create-code:
      referral
      referral-code
    remove-code:
      referral
      referral-code
  token:
    buy-tokens:
      user-token
    update-balance:
      user-token
  user:
    create-user:
      user
    delete-user:
      user
    patch-user:
      *
    add-favorite:
      vendor
      favorite
    remove-favorites:
      vendor
      favorite
  vendor:
    add-vendor-categories:
      vendor
    patch-vendor:
      vendor
    
    